```
p_scan:
    movea.l  0x80, A0         ; A0 = адрес порта ввода (0x80)
    move.l   0, D5            ; D5 = 0 — текущее упакованное слово (без длины)
    move.l   0, D4            ; D4 = 0 — длина текущего слова
    move.l   0, D3            ; D3 = 0 — счётчик прочитанных символов строки
    move.l   0, D2            ; D2 = 0 — статус ошибки (0 = OK)

    ; D1 - длина текущего слова
    ; D7 - 
```