## Стековый процессор [[^ref-cpo]]

Сегодня невозможно представить программирование без стеков. Стек — это одна из простейших структур данных, которая выступает коллекцией элементов с двумя основными операциями:

- `push`, которая добавляет элемент в коллекцию
- `pop`, которая удаляет последний добавленный элемент из коллекции.

![_Stack Data Structure (c)_](fig/stack.jpg){#fig:stack-structure}
<!-- for reference: [figure](#fig:stack-structure) -->

Но эта простая структура позволяет нам выполнять вызовы процедур, автоматическое выделение памяти, определение контекстов переменных и многие другие важные программные вещи.

Лучшим примером организации вычислительного процесса через стек является язык программирования Forth.

> Форт (англ. Forth) — императивный язык программирования на основе стека и среда, первоначально разработанная Чарльзом "Чаком" Муром. Функции языка включают структурное программирование, отражение (возможность исследовать и изменять структуру программы во время выполнения), последовательное программирование (функции составляются с сопоставлением) и расширяемость (программист может создавать новые команды). Хотя это и не аббревиатура, название языка иногда пишется заглавными буквами как FORTH, следуя обычному использованию в более ранние годы.
>
> --- Wikipedia

Поясним организацию вычислительного процесса через стек на примере вычисления факториала (алгоритм см. в листинге).

``` forth
: fac recursive
  dup 1 > IF
    dup 1 - fac *
  else
    drop 1
  endif ;
```

Давайте пошагово рассмотрим вычислительный процесс для выражения `3 fac .`. Все элементы в списке следуют за текущим состоянием стека данных.

- `[]` Положить `3` в стек.
- `[3]` Выполнить слово (на другом языке мы называем его "процедура") `fac`.
  `[3]` Выполнить слово `dup` --- дублирует самый верхний элемент стека.
    - `[3, 3]` Положить `1`.
    - `[3, 3, 1]` Выполнить слово `>` --- извлекает два значения из стека, сравнивает их и возвращает результат обратно.
    - `[3, true]` Извлечь значение из стека и выполнить `dup 1 - fac *`  потому что оно `true`.
    - `[3]` Выполнить слово `dup`.
    - `[3, 3]` Положить `1`.
    - `[3, 3, 1]` Выполнить слово `-` --- извлекает два значения из стека, вычитает их и возвращает результат обратно.
    - `[3, 2]` Выполнить слово `fac` рекурсивно.
        - ...
            - `[3, 2, 1]` Выполнить слово `dup`.
            - `[3, 2, 1, 1]` Положить `1`.
            - `[3, 2, 1, 1, 1]` Выполнить слово `>`.
            - `[3, 2, 1, false]`  Извлечь значение из стека и выполнить `drop 1` потому что `false`.
            - `[3, 2, 1]` Удалить одно значение из стека.
            - `[3, 2]` Положить 1. Почему мы удаляем `1` и кладем `1` снова? Нам это нужно на тот случай, если кто-то попытается вычислить `0 fac .`.
            - `[3, 2, 1]` Возврат.
        - ...
    - `[3, 2]` Выполнить слово `*` --- взять два значения из стека, перемножить их и поместить результат обратно.
    - `[6]`.

Сегодня язык программирования Forth в основном используется в качестве встроенного языка для пользовательской настройки (например, проект Open Firmware), поскольку он прост в реализации. Кроме того, некоторые проекты все еще используют его в качестве основного языка программирования.

Архитектура процессора:

![_Stack Machine (c)_](fig/stacks-machine.gif){#fig:stacks-machine}
<!-- for reference: [figure](#fig:stacks-machine) -->

Безумный стековый процессор из реального мира: [GA144](https://cyberleninka.ru/article/n/protsessory-greenarrays-ga144/pdf)

![_GA144_](fig/GA144.jpg){#fig:ga144}
<!-- for reference: [figure](#fig:ga144) -->

где каждый F18A --- асинхронный форт процессор.

Пример реализации интерпретатора языка программирования Forth от Игоря Олеговича Жиркова: <https://github.com/sayon/forthress-2>

## Reduced Instruction Set Computer (RISC)

![_RISC_](fig/risc.png){#fig:risc}
<!-- for reference: [figure](#fig:risc) -->

Если для CISC процессоров ключевой технологией стал микрокод, то для RISC процессоров ими стали языки высокого уровня, которые позволили разработчикам ПО уйти от написания исходного кода на уровне ассемблера, а значит, фокус актуальных вопросов сменился с удобства написания кода на удобство генерации кода.

Основная идея RISC процессора заключается в том, что лучше сделать минимальный набор унифицированных команд, ориентированных на быстрое исполнение, а "сложное поведение" реализовывать их последовательностями. Это позволило:

- освободить в процессоре площадь от элементов сложных декодеров команд, что позволило расширить регистровые файлы;
- освободить большое количество памяти микрокоманд для кеша команд, что отчасти компенсировало необходимость более частой выборки команд;
- добиться малого количества быстро исполняемых команд;
- "однообразность" команд открыла возможность к оптимизациям производительности на уровне инструкций (примечательно, что традиционный пример RISC процессора, приведенный ниже, уже имеет конвейерную организацию).

![CISC vs RISC instruction_](fig/cisc-risc-instruction.png){#fig:cisc-risc-instruction}
<!-- for reference: [figure](#fig:cisc-risc-instruction) -->

> Инструкции CISC могут быть любой длины. Максимальная теоретическая длина инструкции x86 может быть бесконечной, но на практике не превышает 15 байт. Инструкции RISC имеют ограниченную длину. [Что означает RISC и CISC?](https://habr.com/en/company/selectel/blog/542074/)

Ещё одним из "триггеров" к формированию RISC процессоров стала необходимость длительной отладки машинного кода. Кроме того, практика показала, что переход от "большого CISC" процессора к "маленькому RISC" процессору не только радикально снизил сложность разработки последнего ("то, что раньше делали корпорации, теперь стало доступно для двух аспирантов"), но и повысил его производительность для "средней программы", так как большинство "сложных команд" относительно редко использовалось на практике, а значит, их менее эффективная реализация (в вакууме) не могла заметно снизить общую производительность системы.

Другим, на первый взгляд неочевидным, следствием простоты и компактности RISC процессоров стало их широкое использование в рамках систем на кристалле, так как включить в неё ещё один RISC процессор --- несложная на сегодня задача, при помощи которой можно реализовывать "сервисные модули", а также RISC процессор довольно просто адаптировать под конкретные нужды при помощи специализированных расширений.

Описанный выше текст намекает, что RISC процессоры превосходят CISC процессоры со всех точек зрения. Почему же на сегодня они не заняли рынок полностью (хотя нужно отметить, что все тенденции к этому на сегодня уже намечены, среди которых встроенные системы, мобильные устройства, персональные компьютеры, а также серверные платформы для облачных решений)?

На защите текущего положения CISC процессоров стоит два фактора:

- обратная совместимость, в первую очередь на персональных компьютерах (огромное количество написанного ПО должно работать как раньше, смена архитектуры не позволит использовать привычное ПО или снизит его производительность, что будет неприемлемо для большинства пользователей, в качестве примера можно посмотреть на первые попытки перехода Windows на ARM где-то около 2016 года);
- производство процессоров очень сильно зависит от серийности, в результате складывается немного парадоксальная ситуация, в рамках которой лидер рынка (самый большой сбыт) будет иметь самую маленькую себестоимость производства, как следствие, преследователи будут находиться в заведомо проигрышной ситуации;
- есть основания полагать, что многие современные CISC процессоры содержат внутри так называемое RISC ядро, которое исполняет наборы инструкций, генерируемых в результате интерпретации CISC команд.

- Если RISC так хороши, то почему CISC до сих пор так популярен?
    - обратная совместимость (ПК, у серверов все немного не так);
    - лидер рынка имеет самую большую серийность, поэтому недорогое производство.

<!-- $\frac{Время}{Программа} = \frac{Инструкции}{Программа} * \frac{тактов}{Инструкция} * \frac{Время}{такт}$ -->
<!-- > Время/Программа = (Инструкции / Программа) × (тактов / Инструкция) × (Время / такт) -->

![_Схема традиционного RISC процессора_](fig/RISC-processor-example.png){#fig:risc-example}
<!-- for reference: [figure](#fig:risc-example) -->

### Параллелизм уровня инструкций

Одним из ключевых элементов архитектуры процессора фон Неймана является представление алгоритма в виде последовательности команд, что говорит о том, что инструкции не могут быть выполнены одновременно. На самом деле это не так и параллелизм может быть найден за счёт:

- выполнения команды --- длительный и сложный процесс, значит он может быть разделён на этапы и запущен на конвейере;
- выполнения основной задачи команды (к примеру, деления), что может требовать большого количества времени, а, значит, в процессоре будут присутствовать простаивающие элементы, которые можно загрузить другими задачами командами;
- изменения порядка выполнения команд, что, зачастую, не изменяет их результата.

### Конвейеризированное исполнение команд

Данный тип параллелизма аналогичен изобретению Генри Форда, который предложил реорганизовать производство с цехового принципа (каждый цех выполняет полный цикл по отношению к заготовке) на конвейерный (каждый пост конвейера выполняет работы параллельно над своей заготовкой). Конвейерная организация работ позволяет за счёт реорганизации рабочего пространства и увеличения количества рабочих (пропорционально количеству постов, но не путём простого умножения) повысить производительность. С точки зрения цифровой схемотехники, данный способ эксплуатирует возможность параллельной и независимой работы комбинационных схем, разделённых регистрами, сдвиг же конвейера реализуется по глобальному тактовому сигналу.

Для организации конвейерной работы процессора необходимо (впрочем, как и на классическом производстве):

- выделить стадии выполнения команд;
- организовать внутренние структуры процессора таким образом, чтобы:
    - у процессора был входной (поступают команды) и выходной конец (готовые команды "покидают" конвейер процессора);
    - структура процессора должна соответствовать стадиям выполнения команд, каждый сегмент должен быть связан с последующим через регистровый интерфейс, причём длительность комбинационных схем каждого сегмента должна быть по возможности одинаковой;
    - все сегменты процессора должны управляться от одного тактового сигнала;
- настроить управление конвейером таким образом, чтобы загружать в него команды каждый такт;
- разрешить конфликты и противоречия, вызванные частично параллельным исполнением команд.

Рассмотрим эти шаги подробнее. В качестве стадий выполнения команд возьмём классический конвейер RISC процессора. Он имеет пятиступенчатый конвейер ([Classic RISC pipeline](https://en.wikipedia.org/wiki/Classic_RISC_pipeline)):

1. Instruction Fetch (Извлечение инструкций). Процессор считывает инструкцию по адресу в памяти, значение которого присутствует в счетчике программ.
2. Instruction Decode (Декодирование инструкций). Команда декодируется и осуществляет доступ к регистровому файлу для получения значений из регистров, используемых в инструкции.
3. Instruction Execute (Выполнение инструкции). На этом этапе выполняются операции. Причём операции данного этапа можно разделить на:

    - Register-Register Operation (Single-cycle latency): Сложение, вычитание, сравнение и логические операции. На этапе выполнения два аргумента были переданы в простой АЛУ, который сгенерировал результат к концу этапа выполнения.
    - Memory Reference (Two-cycle latency): Получение данных из памяти и их загрузка в регистры с учетом формирования физического адреса в памяти выполняемого АЛУ.
    - Multi-cycle Instructions (Many cycle latency): Целочисленное умножение и деление и все операции с плавающей запятой. Могут реализовываться в рамках сопроцессора, сохраняющего результат в отдельные регистры, чтобы освободить конвейер. Длительность выполнения команды зависит от конкретных данных.

4. Memory Access (Доступ к памяти). На этом этапе операнды памяти считываются и записываются в виде/в память, присутствующую в инструкции.
5. Write Back (Написать ответ). На этом этапе вычисленное/полученное значение записывается обратно в регистр, присутствующий в инструкциях.

Структура RISC процессора с конвейерной организацией:

![pipelined-execution-stage.png](fig/pipelined-execution-stage.png)

Количество стадий конвейера может варьироваться в широких пределах. Есть как простые варианты процессоров PIC (2 стадии), так и более сложные (Pentium 4, 20-31). Есть также более экстравагантные специализированные решения, где их количество может достигать сотен (как правило, в таком случае речь идёт о специализированных решениях, где роль АЛУ выполняют самостоятельные процессоры).

Очевидно, что, если бы конвейеризация вычислений реализовывалась без каких-либо трудностей, тогда это было бы универсальным решением, позволяющим повышать производительность за счёт повышения частоты (и энергопотребления). Основную сложность для реализации конвейерных вычислений представляют возникающие при его работе конфликты между стадиями конвейера. Выделяют три группы конфликтов:

#### Структурные конфликты / Structural dependency

> Структурные конфликты возникают из-за конфликтов ресурсов, когда аппаратура не может поддерживать все возможные комбинации одновременно выполняемых команд. Если какая-то комбинация команд не может быть поддержана, то говорят, что процессор имеет структурный конфликт. Наиболее часто структурные конфликты происходят, когда некоторый функциональный блок не полностью конвейеризован. Например, некоторые процессоры совместно используют единый конвейер памяти для данных и команд. В результате, когда команда содержит обращение к памяти данных, она вступает в конфликт с обращением более поздней командой. Чтобы этот конфликт разрешался при обращении к памяти за данными, конвейер приостанавливается на один такт.
>
> В качестве альтернативы такому структурному конфликту разработчик мог бы обеспечить отдельное обращение к памяти команд либо путём разбиения кеша на отдельные кеш команд и кеш данных, либо используя множество буферов, называемыми буферами команд для хранения команд, однако, этого не делается во избежание увеличения стоимости блока.

Пример: ситуация возникновения конфликта при доступе к памяти

| Inst. / Tick | 1       | 2       | 3       | 4         | 5   |
| ------------ | ------- | ------- | ------- | --------- | --- |
| I1           | IF(Mem) | ID      | EX      | ! Mem     |     |
| I2           |         | IF(Mem) | ID      | EX        |     |
| I3           |         |         | IF(Mem) | ID        | EX  |
| I4           |         |         |         | ! IF(Mem) | ID  |

Разрешение конфликта за счёт пропуска операции или "пузырька" (отмечен как `0`), задача которого быть обработанным конвейером, но при этом не выполнять никакой полезной работы:

| Inst. / Tick | 1       | 2       | 3       | 4   | 5   | 6   | 7       |
| ------------ | ------- | ------- | ------- | --- | --- | --- | ------- |
| I1           | IF(Mem) | ID      | EX      | Mem | WB  |     |         |
| I2           |         | IF(Mem) | ID      | EX  | Mem | WB  |         |
| I3           |         |         | IF(Mem) | ID  | EX  | Mem | WB      |
| I4           |         |         |         | `0` | `0` | `0` | IF(Mem) |

#### Конфликты по данным / Data Dependency / Data Hazard

> Конфликты по данным возникают, когда зависимость команды от результатов предыдущей проявляется при совмещении команд в конвейере. Данные конфликты происходят, когда конвейер изменяет порядок обращений считывания/записи к операндам так, что он отличается от порядка, который существует для последовательно выполняемых команд в процессоре без конвейера. Существует метод устранения конфликта по данным: форвардинг (англ. register forwarding).

Опасность для данных возникает, если при параллельной работе с ними одна из стадий конвейера осуществляет запись. Игнорирование потенциальных опасностей для данных может привести к возникновению гонок (как при многопоточном программировании). Существует три ситуации, в которых может возникнуть конфликт по данным:

- Чтение после записи (RAW --- Read after Write)

    ```text
    i1. R3 <- R3 - R4
    i2. R11 <- R10 + R3
    ```

    ![Data hazard in the pipelined execution](fig/pipeline-data-hazard.png)

- Запись после чтения (WAR --- Write after Read);

    ```text
    i1. R4 <- R1 + R5
    i2. R5 <- R1 + R2
    ```

- Запись после записи (WAW --- Write after Write).

    ```text
    i1. R2 <- R4 + R7
    i2. R2 <- R1 + R3
    ```

Механизмы разрешения конфликтов:

- вставка пузыря в конвейер (см. выше);
- исполнения не по порядку, out-of-order --- изменение порядка выполнения команд с учётом конвейерного исполнения на уровне компиляции или процессора (см. далее, суперскалярные архитектуры);
- проброс операндов между стадиями процессора --- для выполнения операций без задержек данные могут быть переданы напрямую между стадиями конвейера, не дожидаясь сохранения в регистровый файл или память;
- [переименования регистров](https://ru.wikipedia.org/wiki/Переименование_регистров) --- в случае, если зависимость по данным ложная (WAW, который может быть оптимизирован), промежуточный результат одной из записей может быть переназначен на другой регистр на лету.

#### Конфликты по управлению / Control Dependency / Branch Hazards

Этот тип конфликта возникает во время исполнения команд условного и безусловного перехода, вызова процедур и т.п. Выполнение данного типа операций требует перезагрузки конвейера, так как в него попадают те инструкции, которые не должны быть выполнены. Наибольшую сложность в данном случае представляют операции условного перехода, так как они не позволяют заранее узнать, как будет изменён PC (счётчик команд).

Сброс конвейера --- крайне дорогостоящая операция, частое наступление которой может значительно сократить эффект от использования конвейера, особенно если в нём много стадий.

Решение этой проблемы лежит в области предсказания будущих переходов. Для безусловных --- ранее извлечение информации об условном переходе и проброс её на стадию выборки инструкции. Для условных --- спекулятивные вычисления, которые выполняются на основании наиболее вероятного перехода. Именно последнее является критически важным с точки зрения эффективности использования "длинных конвейеров", так как ошибка в предсказателе --- это потеря времени (сброс конвейера), потеря энергии (произведены не требующиеся расчёты), дополнительный фактор непредсказуемости временных задержек.

Для самостоятельного ознакомления:

- [Spectre](https://en.wikipedia.org/wiki/Spectre_(security_vulnerability))

#### Использование конвейера

Преимущества: повышение производительности и уровня утилизации вычислительных ресурсов.

Потенциальные недостатки конвейера:

- снижение скорости исполнения отдельной команды (за счёт плохой балансировки стадий и дополнительных регистров);
- не все операции могут пройти стадию конвейера за один машинный цикл (загрузка ресурсов);
- необходимость разрешения конфликтов (загрузка ресурсов);
- непредсказуемое время исполнения;
- противоречие с фон Неймановской архитектурой (принцип единой памяти);
- уязвимости, связанные с доступом через "косвенные каналы".

Для повышения эффективности работы конвейера могут применяться множество оптимизаций уровня компилятора. Типичный пример: сортировка инструкций с целью минимизации структурных конфликтов ([Loop unrolling](https://en.wikipedia.org/wiki/Loop_unrolling)).

```c
// before
for (int x = 0; x < 100; x++) {
    delete(x);
}

// after
for (int x = 0; x < 100; x += 5 ) {
    delete(x);
    delete(x + 1);
    delete(x + 2);
    delete(x + 3);
    delete(x + 4);
}
```

Одной из оптимизаций является HyperThreading:

![HyperThreading](fig/hyper-threading.png)
